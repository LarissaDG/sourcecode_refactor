# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# EXP 5a â€” AnÃ¡lise temporal: consistÃªncia de frames contÃ­guos
# Frames sequenciais de vÃ­deos de speed-drawing â†’ Score
# Objetivo: mostrar que frames consecutivos tÃªm notas parecidas
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
experiment:
  name: "exp5a_temporal"

dataset:
  name: video
  path: "/snfs1/speed/larissa.gomide/data/temporal/"

pipeline:
  steps:
    sampling:   true
    captioning: false
    generation: false
    samples:    true
    scoring:    true

sampling:
  strategy: "sequential"
  # ~24 frames/video (fixo pela extracao, ver download_timecraft.py) x quantos
  # videos o download_timecraft.py conseguir baixar (ate 83, depende do que
  # ainda esta disponivel no YouTube). Deixado generoso para nao truncar o
  # dataset real caso o numero de videos baixados varie.
  n_samples: 3000

scoring:
  weights_dir: "/sonic_home/larissa.gomide/APDDv2/modle_weights/"
