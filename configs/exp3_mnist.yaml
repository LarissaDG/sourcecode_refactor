# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# EXP 3 â€” MNIST: score only (sem caption nem geraÃ§Ã£o)
# Objetivo: mostrar que ArtCLIP dÃ¡ notas baixas para dÃ­gitos (nÃ£o-artÃ­stico)
# Contrastar com scores humanos do APDDv2 (exp1)
# â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
experiment:
  name: "exp3_mnist"

dataset:
  name: mnist
  path: "/sonic_home/larissa.gomide/sourcecode_refactor/data/mnist/"

pipeline:
  steps:
    sampling:   true
    captioning: false
    generation: false
    scoring:    true

sampling:
  n_samples: 500
  strategy: "stratified"

scoring:
  weights_dir: "/sonic_home/larissa.gomide/APDDv2/model_weights/"
