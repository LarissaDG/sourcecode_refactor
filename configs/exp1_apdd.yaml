# ─────────────────────────────────────────────
# EXP 1 — APDDv2: pipeline completo
# ─────────────────────────────────────────────
experiment:
  name: "exp1_apdd"

dataset:
  name: apdd
  path: "/sonic_home/larissa.gomide/data/apddv2/"

pipeline:
  steps:
    sampling:   true
    captioning: true
    generation: true
    scoring:    true

sampling:
  n_samples: 500
  strategy: "uniform_bins"   # média dos 9 atributos estéticos, amostrada uniformemente em 30 bins
  n_bins: 30

