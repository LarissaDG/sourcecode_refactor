# ── pipeline/scoring.py ───────────────────────────────────────────────────────

def run_scoring(cfg, data: list) -> None:
    """
    Caixinha 4 — Avaliação com ArtClip.
    Recebe os dados (formato depende das caixinhas anteriores ativas)
    e salva os scores em output_dir.
    """
    # lógica de scoring com ArtClip
    pass