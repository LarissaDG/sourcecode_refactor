# ── pipeline/generation.py ────────────────────────────────────────────────────

def run_generation(cfg, data: list) -> list:
    """
    Caixinha 3 — Geração de imagens a partir de descrições.
    Recebe lista de (imagem_original, caption), devolve lista de
    (imagem_original, caption, imagem_gerada).
    """
    # lógica de geração de imagens
    pass